[project]
name = "api"
version = "0.1.2"
description = "API Server for catsarebeautiful.de"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "ruff>=0.12.10",
    "sqlmodel>=0.0.24",
]
